<UserControl
    x:Class="RongYokDownloader.Views.LibraryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="clr-namespace:RongYokDownloader.Models"
    xmlns:conv="clr-namespace:RongYokDownloader.Converters"
    FontFamily="{StaticResource AppFont}">

    <DockPanel Margin="28,24,28,0">

        <Grid DockPanel.Dock="Top" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="คลังของฉัน" Style="{StaticResource H1}" />
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Muted}" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,8,0">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Series}">
                        <Border Style="{StaticResource Card}" Width="200" Margin="0,0,16,18" Padding="0" ClipToBounds="True">
                            <StackPanel>
                                <Grid Height="270">
                                    <Border CornerRadius="14,14,0,0" Background="{StaticResource BgElevated}" ClipToBounds="True">
                                        <Image conv:ImageLoader.SourceUrl="{Binding DisplayImageUrl}" Stretch="UniformToFill" />
                                    </Border>
                                    <Border VerticalAlignment="Top" HorizontalAlignment="Left" Margin="8"
                                            CornerRadius="6" Background="#CC241247" Padding="7,3">
                                        <TextBlock Text="{Binding TypeThai}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentHover}" />
                                    </Border>
                                </Grid>
                                <StackPanel Margin="13,12,13,13">
                                    <TextBlock Text="{Binding CleanTitle}" FontSize="14" FontWeight="SemiBold"
                                               TextWrapping="Wrap" MaxHeight="42" TextTrimming="CharacterEllipsis" />
                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Style="{StaticResource PrimaryButton}" Padding="0" Height="36" Width="118"
                                                Command="{Binding DataContext.PlayCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="13" VerticalAlignment="Center" Margin="0,0,7,0" />
                                                <TextBlock Text="เล่น" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource GhostButton}" Padding="0" Height="36" Width="36" Margin="6,0,0,0"
                                                ToolTip="เปิดโฟลเดอร์" Content="&#xE838;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Style="{StaticResource GhostButton}" Padding="0" Height="36" Width="36" Margin="6,0,0,0"
                                                ToolTip="เปิดสารบัญ (index.html)" Content="&#xE8A5;" FontFamily="Segoe MDL2 Assets"
                                                Command="{Binding DataContext.OpenTocCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
